''' initializing tests package

One should run the tests by running in upper directore ../nosetests -v to perform
the tests present in this folder

the testmodules are named as test<module>.py which tests that module
in the testmodules: test_<function> tests <function> in that module

'''
